/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'www.re-thinkingthefuture.com', 'www.knic.co.id','vsl2020.com'],
      },
}

module.exports = nextConfig
